#Spring boot configurations
#logging.config=log4j-shopizer.properties
# Use PORT environment variable for cloud deployments (Render, Heroku, etc.)
# Falls back to 8080 for local development
server.port=${PORT:8080}


#Turn Statistics on
#spring.jpa.properties.hibernate.generate_statistics=true
#default database schema
spring.jpa.properties.hibernate.default_schema=SALESMANAGER
#logging.level.org.hibernate.stat=debug

# Database Configuration - Uses environment variables for production
# For local development, set these in your IDE or use default values below
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.ugwlsxuyxsumxingkkjs.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:t6Zy2.M&*$Ett7R}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration for PostgreSQL
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQL10Dialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}

# Enable logging to verify that HikariCP is used, the second entry is specific to HikariCP
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.zaxxer.hikari.HikariConfig=INFO
#TRACE will print binding
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO

#when running from IDE (eclipse) or from springboot:run command
#server.contextPath=/shopizer

logging.level.org.springframework=INFO
logging.level.com.salesmanager=DEBUG
logging.level.com.shopizer=DEBUG
logging.level.org.hibernate=INFO
logging.level.org.apache.http=ERROR

logging.file.name=shopizer.log

#Default Spring white label error page
server.error.whitelabel.enabled=false

spring.servlet.multipart.max-file-size=4MB
spring.servlet.multipart.max-request-size=10MB


#hibernate 5
spring.jpa.hibernate.use-new-id-generator-mappings=true


#Spring boot 2.X
spring.main.allow-bean-definition-overriding: true

#build informations
application-description=@project.description@
application-version=@project.version@
build.timestamp=@maven.build.timestamp@

#actuator
management.endpoints.web.exposure.include=*

management.endpoint.health.show-details=always
management.endpoint.health.show-components=always

management.health.elasticsearch.enabled=false
management.health.mail.enabled=false
management.health.ping.enabled=true

# CORS Configuration
# Comma-separated list of allowed origins for production
# Example: https://your-frontend.vercel.app,https://www.yourdomain.com
# Use * for development (allows all origins)
cors.allowed.origins=${ALLOWED_ORIGINS:*}








